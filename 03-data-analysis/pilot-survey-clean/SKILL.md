---
name: pilot-survey-clean
agent_created: true
summary: 问卷/量表数据清洗 + 信效度分析（α/KMO/EFA/CITC）+ 自包含离线 HTML 仪表盘。适用于本地 docx 问卷 + xlsx/csv 回收数据。
description: >
  问卷与量表数据的清洗和统计分析流水线。当用户提供问卷（docx）与回收数据（xlsx/csv），
  或需要做信度、效度、因子分析、结构方程建模时使用。
  触发词（中英文均触发）：信度、Cronbach α / alpha、KMO、Bartlett 球形检验、EFA、探索性因子、强制提取因子、
  CITC、修正项总相关、题项分析、区分度、极端组 t 检验、效度、SEM、结构方程、CFA、验证性因子、
  Likert 量表、预调查、pilot study、数据清洗、直线作答、缺失值、逻辑矛盾、重复提交、正大杯、SPSS、
  三线表报告、统计报告排版、数据版本对比。
  产出：清洗后数据集 + 纯内联 SVG 的离线 HTML 仪表盘（零外部依赖）。
  不适用于：非问卷类的通用数据处理（那种情况直接写 pandas 脚本，不建 skill）。
---

# 预调查数据清洗与可视化流水线

## 适用场景
- 拿到 `问卷.docx` + `回收数据.xlsx`（问卷星/腾讯问卷等导出的"简答文本+选项分列"格式）。
- 目标是预调查（pilot）：验证问卷可用性、信度、题项区分度，找出需修订的题项，而不是单纯删人。

## 关键判断：哪些该"剔除"，哪些该"预警"
预调查里**不要轻易删样本**。区分两类：
- **硬性无效（直接剔除）**：必答项缺失、李克特全卷无变异（直线作答）、完全相同的重复提交、被筛查题(Q5"从未见过"类)筛除却仍作答后续量表。
- **软性矛盾（保留为"待复核预警"，不删）**：如"自报从未分享"却"分享行为量表偏高"——多为题项口径歧义，正是预调查要发现的修订点。在报告中标注并给出"剔除后"对比，让研究者看到影响。

## 标准分析项
1. **完整性**：缺失、直线作答、重复、筛查违规计数。
2. **作答质量**：完成时长分布（过快<90s 预警）、逻辑矛盾清单。
3. **信度 Cronbach's α**：按构念计算；同时给"全样本"与"剔除矛盾样本后"两组对比（矛盾样本常是拉低信度的主因）。
4. **题项分析**：CITC（修正项-总相关，<0.3 建议删）、极端组 t 检验（高/低 27% 分组，|t|≥3 优）。
5. **描述性统计**：人口统计、多选频率、构念均值。
6. **修订建议**：列出需修的构念/题项与加注意力陷阱题、扩样本量等。

## 实施步骤（Python，隔离 venv）
- 用 openpyxl 读 xlsx（常有 2 行表头：完整题面 + 题号 Q1..Qn，数据从第 3 行起）；python-docx 读问卷结构确认题项与选项。
- 李克特文本如 "Agree (4)" 用正则 `\((\d+)\)` 提数值；多选列是 0/1。
- 用 pandas + numpy 手动算 α、CITC、极端组 t（无需 pingouin/scipy：α=(k/(k-1))(1−Σvar_i/var_total)，CITC=corr(item, total−item)，t 用高低 27% 分组 Welch 近似，规则 |t|≥3 显著）。
- **可视化优先用纯内联 SVG**（自写 hbar/grouped_hbar/donut/vbar/hist 函数），不要依赖 Chart.js CDN——沙箱网络常受限，离线必定渲染。深色主题配色：BG #0d1117 / 卡片 #161b22 / 强调蓝 #58a6ff 绿 #3fb950 琥珀 #d29922 红 #f85149。
- 输出 `index.html`（自包含，内嵌 SVG+表格+结论）、`cleaned_data.csv`（有效样本 + 各构念均值列）、`analysis.json`（中间数据）。

## 产物结构（建议）
```
pilot_clean/
  index.html          # 仪表盘：KPI + 数据质量 + 信度 + 题项分析 + 描述统计 + 修订建议
  cleaned_data.csv    # 清洗后数据集
  analysis.json       # 中间统计
```

## 报告生成（三线表 docx）硬规则
Kelsen 的问卷项目会反复换数据版本重跑同一份报告，以下四条是踩过坑的硬约束：

1. **脚本必须命令行传参**：`python 分析.py <输入xlsx> <输出目录>` / `python 报告.py <结果目录> [输出文件名]`。
   一套脚本服务多个数据版本，结果落在独立目录（`output_v6/`、`output_v6b/`），旧版本目录一个字不动。
2. **同名数据文件可能内容不同**！用户给的 `xxx_v6.xlsx` 与上次那份常是不同修订版。
   开工第一步用 MD5 + 逐格比对确认（题项单元格差异数、涉及样本数），并在交付时列出「本次 vs 上次」关键指标对照，证明没复用旧结果。
3. **报告正文里的任何数值必须从结果 CSV 动态计算，禁止硬编码**。最容易写死且换数据后必然失真的位置：
   总体 α、KMO、载荷区间、最大交叉载荷、R²、特征值是否全 >1、最强路径、因子编号→构念映射。
   凡是结论性表述都要带分支（例：α 是否全部 ≥0.80；第 8 个特征值是否 >1，若 <1 则改述为"按理论强制提取"）。
4. **因子编号（F1/F2/…）→ 理论构念的映射每次旋转都可能变**（同一份问卷两个版本就可能 F1=SV→IV 互换）。
   必须按各题主载荷绝对值自动归属，不能沿用上一次的映射。
- 三线表样式：顶线与底线 1.5pt（`w:sz=12`）、栏目线（表头下）0.5pt（`w:sz=4`）、无竖线（`w:val=nil`）。
- 数值格式用 `fmt3` 去尾零（0.790→0.79、61.60→61.6），这是既定规则；核对时要用同口径，否则会把"格式差异"误判成"数值缺失"。
- 交付前全量校验：结构（表格数/解读条数/章节齐全）→ 关键表逐值对 CSV → 旧值残留扫描 → 边框样式。
- **Edit 工具对本项目长报告脚本的修改会间歇性丢失**（报告 success 但未落盘）。改用 python 读文件→`str.replace`→写回，并在同一条命令内重新读取校验。`grep -c` 计数为 0 时退出码为 1，用 `&&` 串联会静默中断后续命令，注意改用 `;`。

## 易错点
- 脚本不要命名为 `inspect.py`（与标准库冲突导致 openpyxl 循环导入报错）。
- 新 pandas 已移除 `DataFrame.applymap`，改用 `df.apply(lambda c: c.map(f))`。
- xlsx 表头列名往往与题号不同（如"作答总时长(秒)"实际列头是"作答总时长"），以 codes 行(R2)为准。
- 沙箱 pip 安装很慢（pandas 约 10+ 分钟），用隔离 venv：`.../binaries/python/envs/default/Scripts/pip.exe`。
